# 📋 بررسی کامل کدهای SalesFlow AI

**تاریخ ایجاد:** ۱۴۰۴/۰۹/۰۳  
**مخزن:** ReZaiden/SalesFlow-AI-with-Python  
**زبان بررسی:** فارسی

---

## 🎯 خلاصه پروژه

**SalesFlow AI** یک اپلیکیشن فروش مبتنی بر هوش مصنوعی است که با پایتون نوشته شده و قابلیت‌های زیر را دارد:

- گفتگوی طبیعی با مشتریان با استفاده از مدل‌های GPT اوپن‌ای‌آی
- فیلتر و پیشنهاد محصولات از فایل‌های اکسل، PDF و TXT
- ثبت خودکار اطلاعات مشتریان (ایمیل/تلفن)
- ارسال اطلاعیه‌های لحظه‌ای از طریق ntfy.sh
- رابط کاربری وب زیبا با Gradio

---

## 📁 ساختار پروژه

```
SalesFlow-AI-with-Python/
├── app.py                  # نقطه ورود اصلی برنامه
├── config.yaml             # تنظیمات برنامه
├── requirements.txt        # وابستگی‌های پایتون
├── .env.sample            # قالب متغیرهای محیطی
├── .gitignore             # قوانین نادیده‌گرفتن گیت
├── README.md              # مستندات پروژه
│
├── src/                   # ماژول‌های کد اصلی
│   ├── __init__.py       # فایل مقداردهی بسته (خالی)
│   ├── config.py         # مدیریت تنظیمات
│   ├── logger.py         # سیستم لاگ‌گیری متمرکز
│   ├── data.py           # بارگذاری پایگاه دانش
│   ├── tools.py          # ابزارهای هوش مصنوعی
│   ├── agent.py          # عامل چت اوپن‌ای‌آی
│   └── ui.py             # رابط کاربری Gradio
│
├── tests/                 # مجموعه تست‌ها
│   ├── test_config.py    # تست تنظیمات
│   ├── test_logger.py    # تست لاگر
│   ├── test_data.py      # تست بارگذار داده
│   ├── test_tools.py     # تست ابزارها
│   ├── test_agent.py     # تست عامل
│   └── test_ui.py        # تست رابط کاربری
│
└── files/                 # فایل‌های پایگاه دانش
    ├── products.xlsx      # کاتالوگ محصولات (اکسل)
    ├── info.pdf          # اطلاعات محصولات (PDF)
    ├── product_summary.docx # خلاصه محصولات (ورد)
    └── summary.txt       # اطلاعات شرکت (متن)
```

---

## 🔍 تحلیل دقیق فایل‌ها

### 1. **app.py** - نقطه ورود برنامه

**هدف:** راه‌اندازی و اجرای سرور Gradio

**ویژگی‌های کلیدی:**
- وارد کردن و مقداردهی اولیه لاگر
- بارگذاری تنظیمات سرور از config.yaml
- راه‌اندازی رابط Gradio روی هاست/پورت مشخص‌شده
- مدیریت خطاهای راه‌اندازی

**ساختار کد:**
```python
- setup_logger("app") → مقداردهی لاگ‌گیری
- Config().get()['server'] → بارگذاری تنظیمات سرور
- Interface.launch() → شروع سرور Gradio
- مدیریت استثناها برای خطاها
```

**نکات مهم:**
- یک اشتباه تایپی دارد: "Luch" باید "Launch" باشد (خط ۹)
- پیش‌فرض سرور: localhost:8000

---

### 2. **config.yaml** - تنظیمات برنامه

**هدف:** تنظیمات متمرکز YAML برای همه بخش‌های برنامه

**بخش‌های تنظیمات:**

1. **تنظیمات سرور:**
   - نام: "localhost"
   - پورت: 8000

2. **تنظیمات عامل:**
   - نام: "AidenAI"
   - نقش: "مشاور فروش"
   - شرکت: "شاپ آیدن"

3. **منابع داده:**
   - فایل اکسل: "files/products.xlsx"
   - فایل PDF: "files/info.pdf"
   - فایل متنی: "files/summary.txt"

4. **تنظیمات رابط کاربری:**
   - عنوان: "SalesFlow AI - AidenAI"
   - توضیحات: "مشاور فروش هوش مصنوعی شاپ‌آیدن"
   - تم: "Ocean"
   - ذخیره تاریخچه: فعال

---

### 3. **src/config.py** - مدیر تنظیمات

**هدف:** بارگذاری متغیرهای محیطی و تنظیمات YAML

**اجزای کلیدی:**

1. **متغیرهای محیطی (از .env):**
   - AI_API_KEY - کلید API اوپن‌ای‌آی (الزامی)
   - AI_BASE_URL - URL پایه اوپن‌ای‌آی (اختیاری)
   - AI_MODEL - نام مدل (اختیاری)
   - NTFY_TOPIC - موضوع ntfy.sh (الزامی)
   - NTFY_SERVER - URL سرور ntfy.sh (اختیاری)

2. **متدهای کلاس Config:**
   - `load_yaml()` - بارگذاری فایل config.yaml
   - `get()` - برگرداندن تنظیمات تجزیه‌شده YAML
   - اعتبارسنجی متغیرهای محیطی الزامی در زمان import

3. **ویژگی‌ها:**
   - استفاده از python-dotenv برای بارگذاری .env
   - اعتبارسنجی خودکار متغیرهای حیاتی
   - ایجاد ValueError در صورت نبود AI_API_KEY یا NTFY_TOPIC
   - محاسبه ROOT_DIR برای مسیرهای نسبی

**نکته امنیتی:** هرگز فایل .env را کامیت نکنید (در .gitignore موجود است)

---

### 4. **src/logger.py** - لاگ‌گیری متمرکز

**هدف:** راه‌اندازی لاگ‌گیری یکسان در همه ماژول‌ها

**ویژگی‌ها:**
- ایجاد لاگر با خروجی کنسول و فایل
- ذخیره لاگ‌ها در `logs/{name}.log`
- فرمت: `زمان - نام - سطح - پیام`
- ایجاد خودکار پوشه logs در صورت عدم وجود
- جلوگیری از راه‌اندازی مضاعف
- کدگذاری UTF-8 برای کاراکترهای بین‌المللی

**نحوه استفاده:**
```python
from src.logger import setup_logger
logger = setup_logger("نام_ماژول")
logger.info("پیام")
```

**سطوح لاگ استفاده‌شده:**
- INFO - اطلاعات عمومی
- ERROR - پیام‌های خطا
- WARNING - پیام‌های هشدار

---

### 5. **src/data.py** - مدیر پایگاه دانش

**هدف:** بارگذاری و جستجوی داده محصولات از منابع متعدد

**کلاس: KnowledgeBase**

**ویژگی‌ها:**
- `products`: List[Dict] - کاتالوگ محصولات از اکسل
- `pdf_text`: str - متن استخراج‌شده از PDF
- `txt_text`: str - متن از summary.txt
- `loaded`: bool - پرچم وضعیت بارگذاری

**متدها:**

1. **`load()`** - بارگذاری همه منابع دانش
   - فقط یک‌بار بارگذاری می‌کند (بررسی پرچم `loaded`)
   - بارگذاری فایل‌های Excel، PDF و TXT
   - لاگ موفقیت/شکست برای هر منبع

2. **`_load_excel()`** - بارگذاری محصولات از اکسل
   - استفاده از pandas برای خواندن فایل .xlsx
   - نرمال‌سازی نام ستون‌ها (حروف کوچک، زیرخط)
   - تبدیل به لیست دیکشنری‌ها
   - مدیریت فایل گم‌شده به‌صورت ایمن

3. **`_load_pdf()`** - استخراج متن از PDF
   - استفاده از pypdf.PdfReader
   - استخراج متن از همه صفحات
   - پیوند با خط جدید
   - ذخیره در ویژگی `pdf_text`

4. **`_load_txt()`** - بارگذاری فایل متنی
   - خواندن ساده فایل UTF-8
   - ذخیره در ویژگی `txt_text`

5. **`filter_products()`** - جستجوی محصولات
   - پارامترها: name (str)، min_price (float)، max_price (float)
   - جستجوی نام بدون حساسیت به حروف بزرگ/کوچک
   - فیلتر محدوده قیمت
   - برگرداندن لیست محصولات فیلترشده

6. **`get_all_products()`** - برگرداندن همه محصولات

**جریان داده:**
```
فایل Excel → pandas → نرمال‌سازی → List[Dict]
فایل PDF → pypdf → استخراج متن → string
فایل TXT → open() → read → string
```

---

### 6. **src/tools.py** - ابزارهای تابع هوش مصنوعی

**هدف:** ارائه توابع قابل‌فراخوانی برای عامل هوش مصنوعی

**ابزارهای پیاده‌سازی‌شده:**

1. **`send_notification(title: str, message: str) -> bool`**
   - ارسال اطلاعیه از طریق API ntfy.sh
   - استفاده از درخواست POST به NTFY_SERVER/NTFY_TOPIC
   - اضافه کردن تگ "loudspeaker" به اطلاعیه
   - برگرداندن True در صورت موفقیت، False در صورت شکست
   - لاگ همه تلاش‌های اطلاع‌رسانی

2. **`filter_products(name, min_price, max_price) -> List[Dict]`**
   - پوشش دهنده KnowledgeBase.filter_products()
   - جستجوی کاتالوگ محصولات
   - لاگ تعداد نتایج یافت‌شده
   - برگرداندن لیست محصولات منطبق

**مقداردهی اولیه سطح ماژول:**
- ایجاد نمونه سراسری `knowledge_base`
- پیش‌بارگذاری همه منابع دانش در import
- آماده‌سازی داده برای پرس‌وجوهای فوری

**یکپارچه‌سازی API:**
- استفاده از کتابخانه `requests` برای فراخوانی HTTP
- نقطه پایانی ntfy.sh: `{server}/{topic}`
- متد HTTP: POST با payload داده

---

### 7. **src/agent.py** - عامل چت اوپن‌ای‌آی

**هدف:** موتور اصلی گفتگوی هوش مصنوعی با قابلیت فراخوانی تابع

**اجزای کلیدی:**

1. **راه‌اندازی کلاینت OpenAI:**
   - استفاده از کتابخانه openai نسخه 2.x API
   - پیکربندی با کلید API و URL پایه
   - مدل مشخص‌شده از محیط

2. **یکپارچه‌سازی پایگاه دانش:**
   - بارگذاری KnowledgeBase در import
   - جاسازی محتوای PDF و TXT در prompt سیستم
   - در دسترس قرار دادن اطلاعات محصول برای هوش مصنوعی

3. **تعاریف ابزار (فرمت تابع OpenAI):**

   **send_notification_json:**
   - نام: "send_notification"
   - توضیحات: چه زمانی اطلاعیه ارسال شود
   - پارامترها: title (string)، message (string)
   - مورد استفاده: ثبت مشتری، اطلاعات تماس

   **filter_products_json:**
   - نام: "filter_products"
   - توضیحات: چه زمانی محصولات جستجو شوند
   - پارامترها: name (string)، min_price (int)، max_price (int)
   - مورد استفاده: پرس‌وجوهای محصول، محدوده قیمت

4. **اجرای ابزار:**
   - `handle_tool_calls()` - اجرای ابزارهای درخواست‌شده هوش مصنوعی
   - تجزیه آرگومان‌های JSON
   - فراخوانی تابع پایتون متناظر
   - برگرداندن نتایج در فرمت OpenAI

5. **Prompt سیستم:**
   - تعریف شخصیت عامل به‌عنوان "AidenAI"
   - نقش: مشاور فروش در شاپ آیدن
   - شامل اطلاعات شرکت از txt_text
   - شامل اطلاعات محصول از pdf_text
   - اهداف و سبک ارتباطی واضح
   - تمرکز بر تولید سرنخ فروش
   - راهنماهای جمع‌آوری اطلاعات تماس

6. **تابع چت:**
   - `chat(message: str, history: List) -> str`
   - ساخت آرایه پیام با prompt سیستم + تاریخچه
   - فراخوانی API OpenAI با پارامتر tools
   - مدیریت دلیل پایان tool_calls
   - حلقه تا تکمیل
   - برگرداندن پاسخ نهایی هوش مصنوعی

**جریان هوش مصنوعی:**
```
پیام کاربر → ساخت متن → API OpenAI
           ↓
    نیاز به فراخوانی ابزار؟
       بله → اجرای ابزار → اضافه کردن نتیجه → حلقه برگشت
       خیر → برگرداندن پاسخ
```

**ویژگی‌های مهم:**
- فراخوانی تابع (tools) برای اقدامات
- گفتگوهای چندمرحله‌ای با تاریخچه
- حلقه خودکار اجرای ابزار
- پاسخ‌های زبان طبیعی
- هوش ثبت سرنخ

---

### 8. **src/ui.py** - رابط Gradio

**هدف:** ایجاد رابط کاربری وب برای تعامل چت

**پیاده‌سازی:**
- استفاده از کامپوننت `gradio.ChatInterface`
- نوع: 'messages' (فرمت چت مدرن)
- اتصال به تابع `chat` از agent.py
- بارگذاری تنظیمات UI از config.yaml

**تنظیمات رابط کاربری:**
- عنوان: "SalesFlow AI - AidenAI"
- توضیحات: "مشاور فروش هوش مصنوعی شاپ‌آیدن"
- تم: "Ocean" (تم داخلی Gradio)
- ذخیره تاریخچه: فعال

**ساده و موثر:**
- فقط ۱۹ خط کد
- استفاده از ویژگی‌های داخلی چت Gradio
- نیازی به HTML/CSS سفارشی نیست
- مدیریت خودکار تاریخچه پیام

---

## 🧪 تحلیل مجموعه تست

### test_config.py
**تست‌ها:** بارگذاری متغیر محیطی
- اعتبارسنجی وجود AI_API_KEY
- اعتبارسنجی وجود NTFY_TOPIC
- اطمینان از حضور تنظیمات حیاتی

### test_logger.py
**تست‌ها:** ایجاد لاگر
- تأیید نوع شیء لاگر
- تست عملکرد پایه لاگ‌گیری

### test_data.py
**تست‌ها:** بارگذاری پایگاه دانش
- بارگذاری فایل Excel و فیلتر
- استخراج متن PDF (تعداد کاراکتر > 0)
- بارگذاری فایل TXT (تعداد کاراکتر > 0)
- فیلتر محصولات با پارامترها

### test_tools.py
**تست‌ها:** ابزارهای تابع
- ارسال اطلاعیه (ارسال اطلاعیه تست)
- فیلتر محصولات با نام و محدوده قیمت
- اعتبارسنجی تعداد نتایج

### test_agent.py
**تست‌ها:** عملکرد عامل هوش مصنوعی
- تست پاسخ چت پایه
- فراخوانی ابزار: send_notification
- فراخوانی ابزار: filter_products
- استفاده از prompt‌های خاص برای اعتبارسنجی رفتار

### test_ui.py
**تست‌ها:** رابط Gradio
- راه‌اندازی سرور در thread
- بررسی پاسخ HTTP (وضعیت 200)
- اعتبارسنجی دسترسی سرور
- استفاده از threading برای تست ناهمزمان

**پوشش تست:** پوشش جامع همه ماژول‌های اصلی

**فریمورک تست:** pytest

**سبک تست:** تست‌های یکپارچگی (فراخوانی API واقعی، اطلاعیه‌های واقعی)

---

## 📦 تحلیل وابستگی‌ها

### هسته هوش مصنوعی و یادگیری ماشین:
- **openai==2.8.0** - یکپارچه‌سازی GPT API
- **gradio==5.49.1** - فریمورک رابط کاربری وب
- **gradio_client==1.13.3** - پشتیبانی کلاینت Gradio

### پردازش داده:
- **pandas==2.3.3** - مدیریت فایل Excel
- **numpy==2.3.4** - عملیات عددی (وابستگی pandas)
- **openpyxl==3.1.5** - پشتیبانی فرمت فایل Excel
- **pypdf==6.2.0** - استخراج متن PDF

### فریمورک وب:
- **fastapi==0.121.2** - فریمورک ASGI (وابستگی Gradio)
- **uvicorn==0.38.0** - سرور ASGI
- **starlette==0.49.3** - اجزای فریمورک وب
- **httpx==0.28.1** - کلاینت HTTP
- **websockets==15.0.1** - پشتیبانی WebSocket

### تنظیمات:
- **python-dotenv==1.2.1** - بارگذاری فایل .env
- **PyYAML==6.0.3** - تجزیه YAML

### ابزارها:
- **requests==2.32.5** - درخواست‌های HTTP (ntfy.sh)
- **click==8.3.1** - فریمورک CLI
- **rich==14.2.0** - قالب‌بندی ترمینال

### تست:
- **pytest==9.0.1** - فریمورک تست
- **pluggy==1.6.0** - سیستم افزونه

### سایر قابل‌توجه:
- **huggingface_hub==1.1.4** - میزبانی مدل HF
- **Pillow==11.3.0** - پردازش تصویر
- **ffmpy==1.0.0** - پردازش صوت/ویدیو

**کل وابستگی‌ها:** حدود ۷۰ بسته (شامل وابستگی‌های فرعی)

**نسخه پایتون مورد نیاز:** 3.9+

---

## 🗂️ فایل‌های داده

### files/products.xlsx
- **فرمت:** صفحه گسترده Excel
- **محتوا:** کاتالوگ محصولات با ستون‌ها (نرمال‌شده به حروف کوچک):
  - name - نام محصول
  - price - قیمت محصول
  - (احتمالاً ستون‌های دیگر)
- **استفاده:** پایگاه داده اصلی محصولات برای فیلتر

### files/info.pdf
- **فرمت:** سند PDF
- **محتوا:** اطلاعات و جزئیات محصولات
- **استفاده:** جاسازی در prompt سیستم هوش مصنوعی برای متن
- **پردازش:** استخراج متن از طریق pypdf

### files/summary.txt
- **فرمت:** متن ساده (UTF-8)
- **زبان:** فارسی
- **محتوا:** اطلاعات شرکت درباره شاپ آیدن
  - نام شرکت و وبسایت
  - ماموریت و چشم‌انداز
  - دسته‌بندی محصولات
  - ارزش‌های اصلی
  - ساختار تیم
  - مزایای رقابتی
- **استفاده:** جاسازی در prompt سیستم هوش مصنوعی برای متن شرکت

### files/product_summary.docx
- **فرمت:** سند Word
- **وضعیت:** در حال حاضر توسط برنامه بارگذاری نمی‌شود
- **توجه:** در صورت نیاز می‌تواند یکپارچه شود

---

## 🔐 امنیت و تنظیمات

### متغیرهای محیطی (.env)
**الزامی:**
- `AI_API_KEY` - کلید API OpenAI (حیاتی)
- `NTFY_TOPIC` - موضوع اطلاع‌رسانی (حیاتی)

**اختیاری:**
- `AI_BASE_URL` - نقطه پایانی API سفارشی
- `AI_MODEL` - نام مدل خاص
- `NTFY_SERVER` - سرور ntfy سفارشی (پیش‌فرض: https://ntfy.sh)

### بهترین شیوه‌های امنیتی رعایت‌شده:
✅ فایل .env در .gitignore  
✅ اعتبارسنجی متغیر محیطی  
✅ .env.sample به‌عنوان قالب ارائه شده  
✅ بدون رمزهای سخت‌کدشده در کد  
✅ کدگذاری UTF-8 برای متن بین‌المللی  

### ملاحظات امنیتی:
⚠️ کلید API در صورت کامیت .env آشکار می‌شود (توسط .gitignore جلوگیری شده)  
⚠️ اطلاعیه‌های ntfy.sh عمومی هستند مگر از احراز هویت استفاده شود  
⚠️ محدودیت نرخ برای فراخوانی‌های API وجود ندارد  
⚠️ پاکسازی ورودی برای پیام‌های کاربر وجود ندارد  
⚠️ فراخوانی‌های مستقیم API OpenAI (ملاحظه هزینه)  

---

## 🎨 جریان برنامه

### توالی راه‌اندازی:
```
1. app.py بارگذاری می‌شود
2. Import src.logger → setup_logger("app")
3. Import src.config → بارگذاری .env و config.yaml
4. Import src.ui → بارگذاری تنظیمات UI و ایجاد Interface
5. Interface.launch() → شروع سرور Gradio
6. سرور روی localhost:8000 گوش می‌دهد
```

### جریان تعامل کاربر:
```
1. کاربر http://localhost:8000 را باز می‌کند
2. رابط چت Gradio بارگذاری می‌شود
3. کاربر پیام تایپ می‌کند
4. پیام → ui.py → agent.chat()
5. agent.chat() متن را می‌سازد:
   - Prompt سیستم (با اطلاعات شرکت + محصول)
   - تاریخچه چت
   - پیام کاربر
6. فراخوانی API OpenAI با tools
7. پاسخ هوش مصنوعی یا فراخوانی ابزار:
   - اگر tool_calls: اجرا → اضافه کردن نتیجه → حلقه
   - اگر stop: برگرداندن پاسخ متنی
8. پاسخ در رابط کاربری نمایش داده می‌شود
9. تاریخچه ذخیره می‌شود (در صورت فعال بودن)
```

### جریان اجرای ابزار:
```
1. هوش مصنوعی تصمیم به فراخوانی ابزار می‌گیرد (filter_products یا send_notification)
2. handle_tool_calls() tool_calls را دریافت می‌کند
3. تجزیه نام ابزار و آرگومان‌های JSON
4. فراخوانی تابع پایتون از طریق globals().get()
5. تابع اجرا می‌شود:
   - send_notification → POST به ntfy.sh
   - filter_products → پرس‌وجو پایگاه دانش
6. نتیجه به‌عنوان پاسخ ابزار قالب‌بندی می‌شود
7. به تاریخچه پیام اضافه می‌شود
8. حلقه برگشت به API OpenAI
9. هوش مصنوعی نتیجه را پردازش و به‌صورت طبیعی پاسخ می‌دهد
```

---

## 💡 ویژگی‌ها و قابلیت‌های کلیدی

### 1. **گفتگوی طبیعی**
- استفاده از مدل‌های GPT OpenAI
- نگهداری تاریخچه گفتگو
- پاسخ‌های متناسب با متن
- شخصیت: مشاور فروش دوستانه

### 2. **هوش محصولات**
- بارگذاری محصولات از Excel
- فیلتر بر اساس نام (بدون حساسیت به حروف بزرگ/کوچک)
- فیلتر بر اساس محدوده قیمت
- برگرداندن داده ساختاریافته

### 3. **ثبت سرنخ**
- شناسایی اطلاعات تماس در گفتگو
- ثبت ایمیل و شماره تلفن
- ارسال اطلاعیه به صاحب کسب‌وکار
- رویکرد تولید سرنخ غیرتهاجمی

### 4. **اطلاعیه‌های لحظه‌ای**
- یکپارچه‌سازی ntfy.sh
- اطلاعیه‌های فوری push
- کار روی دستگاه‌های مختلف
- راه‌اندازی رایگان و ساده

### 5. **دانش چندمنبعی**
- کاتالوگ محصولات Excel
- اطلاعات محصولات PDF
- اطلاعات شرکت TXT
- قابل گسترش با منابع بیشتر

### 6. **رابط کاربری زیبا**
- رابط وب Gradio
- طراحی پاسخگو
- تم Ocean
- ذخیره تاریخچه چت
- نیازی به کد فرانت‌اند نیست

### 7. **معماری مدولار**
- جداسازی نگرانی‌ها (config، logging، data، tools، agent، UI)
- آسان برای تست
- آسان برای گسترش
- وابستگی‌های واضح

### 8. **لاگ‌گیری جامع**
- همه عملیات لاگ می‌شوند
- خروجی فایل و کنسول
- ورودی‌های با مهر زمان
- اشکال‌زدایی آسان

---

## 🔧 گزینه‌های تنظیمات

### تنظیمات سرور:
- آدرس هاست/IP
- شماره پورت

### تنظیمات عامل:
- نام عامل
- توضیح نقش
- نام شرکت

### منابع داده:
- مسیر فایل Excel
- مسیر فایل PDF
- مسیر فایل TXT

### تنظیمات رابط کاربری:
- عنوان
- توضیحات
- انتخاب تم
- فعال/غیرفعال ذخیره تاریخچه

### متغیرهای محیطی:
- اعتبار API هوش مصنوعی
- انتخاب مدل هوش مصنوعی
- تنظیمات اطلاع‌رسانی

---

## 🚀 اجرای برنامه

### راه‌اندازی:
```bash
# کلون مخزن
git clone https://github.com/ReZaiden/SalesFlow-AI-with-Python.git
cd SalesFlow-AI-with-Python

# ایجاد محیط مجازی
python -m venv venv
source venv/bin/activate  # در ویندوز: venv\Scripts\activate

# نصب وابستگی‌ها
pip install -r requirements.txt

# تنظیم محیط
cp .env.sample .env
# ویرایش .env با کلیدهای API خود

# اجرای تست‌ها
python -m pytest

# شروع برنامه
python -m app
```

### دسترسی:
- باز کردن مرورگر به http://localhost:8000
- شروع چت با AidenAI
- تست پرس‌وجوهای محصول
- امتحان ارائه اطلاعات تماس

---

## 🎯 موارد استفاده

### 1. **پیشنهاد محصولات**
کاربر: "به یک شارژر گوشی زیر 30 دلار نیاز دارم"  
عامل: جستجوی محصولات → برگرداندن موارد منطبق → توضیح ویژگی‌ها

### 2. **ثبت سرنخ**
کاربر: "علاقه‌مندم، ایمیلم user@example.com است"  
عامل: ثبت سرنخ → ارسال اطلاعیه → تأیید پیگیری

### 3. **پرس‌وجوهای اطلاعاتی**
کاربر: "چه نوع محصولاتی می‌فروشید؟"  
عامل: استفاده از دانش PDF/TXT → ارائه پاسخ دقیق

### 4. **مقایسه قیمت**
کاربر: "همه محصولات بین 20 تا 50 دلار را نشان بده"  
عامل: فیلتر بر اساس محدوده قیمت → ارائه گزینه‌ها

---

## 📊 ارزیابی کیفیت کد

### نقاط قوت:
✅ معماری مدولار تمیز  
✅ پوشش جامع تست  
✅ مستندات خوب (README)  
✅ مدیریت خطای مناسب  
✅ لاگ‌گیری متمرکز  
✅ مدیریت تنظیمات  
✅ راهنماهای نوع در امضاهای تابع  
✅ قراردادهای نام‌گذاری سازگار  
✅ استفاده خوب از ویژگی‌های مدرن پایتون  

### زمینه‌های بهبود:
⚠️ اشتباه تایپی در app.py (خط 9: "Luch" → "Launch")  
⚠️ راهنماهای نوع برای ویژگی‌های کلاس وجود ندارد  
⚠️ مدیریت خطای محدود در اجرای ابزار  
⚠️ منطق تلاش مجدد برای فراخوانی‌های API وجود ندارد  
⚠️ حافظه پنهان برای پایگاه دانش وجود ندارد  
⚠️ سخت برای تست واحد (فقط تست‌های یکپارچگی)  
⚠️ استفاده از async/await وجود ندارد  
⚠️ requirements.txt مشکلات کدگذاری باینری دارد  

---

## 🔮 بهبودهای بالقوه

### کوتاه‌مدت:
1. رفع اشتباه تایپی در app.py
2. اضافه کردن مدیریت خطای قوی‌تر
3. پیاده‌سازی منطق تلاش مجدد API
4. اضافه کردن اعتبارسنجی ورودی
5. حافظه پنهان بارگذاری پایگاه دانش

### میان‌مدت:
1. اضافه کردن جستجوی برداری (ChromaDB) همان‌طور که در README ذکر شده
2. پیاده‌سازی پایگاه داده تاریخچه گفتگو
3. اضافه کردن انواع منبع داده بیشتر
4. ایجاد داشبورد مدیریت
5. اضافه کردن محدودیت نرخ

### بلندمدت:
1. پشتیبانی چندزبانه
2. یکپارچه‌سازی رابط صوتی
3. تجزیه‌وتحلیل و گزارش‌دهی
4. یکپارچه‌سازی CRM
5. آموزش مدل سفارشی

---

## 📝 معیارهای کد

### خطوط کد (تقریبی):
- **app.py**: ۱۴ خط
- **src/config.py**: ۵۲ خط
- **src/logger.py**: ۳۵ خط
- **src/data.py**: ۱۱۵ خط
- **src/tools.py**: ۳۸ خط
- **src/agent.py**: ۱۵۰ خط
- **src/ui.py**: ۱۹ خط
- **تست‌ها**: حدود ۱۰۰ خط در مجموع

**کل کد منبع**: حدود ۵۲۳ خط  
**کل کد تست**: حدود ۱۰۰ خط  
**نسبت پوشش تست**: حدود ۱۹٪

### پیچیدگی:
- **پیچیدگی دورانی**: پایین (عمدتاً جریان خطی)
- **جفت‌شدگی**: متوسط (ماژول‌ها به config/logger وابسته‌اند)
- **انسجام**: بالا (هر ماژول هدف واضحی دارد)

---

## 🌟 نتیجه‌گیری

**SalesFlow AI** یک اپلیکیشن پایتون مدرن و خوب‌ساختار است که نشان می‌دهد:
- استفاده مؤثر از فراخوانی تابع OpenAI
- معماری مدولار تمیز
- جداسازی خوب نگرانی‌ها
- مدیریت تنظیمات مناسب
- رویکرد تست جامع
- کاربرد عملی دنیای واقعی

کدبیس با بهبودهای جزئی برای تولید آماده است. نمونه عالی از یکپارچه‌سازی قابلیت‌های هوش مصنوعی در اپلیکیشن کسب‌وکار با شیوه‌های مهندسی مناسب است.

**رتبه کلی: 8.5/10**

**بهترین شیوه‌های دنبال‌شده:**
- ✅ مدیریت متغیر محیطی
- ✅ طراحی مدولار
- ✅ مدیریت خطا
- ✅ لاگ‌گیری
- ✅ تست
- ✅ مستندسازی

**گام‌های بعدی توصیه‌شده:**
1. رفع اشتباه تایپی شناسایی‌شده
2. اضافه کردن مدیریت خطای بیشتر
3. پیاده‌سازی جستجوی برداری
4. اضافه کردن ماندگاری گفتگو
5. استقرار در محیط تولید

---

**بررسی تکمیل‌شده توسط:** دستیار هوش مصنوعی GitHub Copilot  
**تاریخ:** ۱۴۰۴/۰۹/۰۳  
**وضعیت:** ✅ کامل

---

## 📞 تماس با پروژه

**توسعه‌دهنده:** ReZaiden  
**گیت‌هاب:** [@ReZaiden](https://github.com/ReZaiden)  
**ایمیل:** rezaidensalmani@gmail.com  
**دموی زنده:** [Hugging Face Space](https://huggingface.co/spaces/ReZaiden/SalesAI_Flow)

---

*این بررسی جامع همه جنبه‌های کدبیس SalesFlow AI را پوشش می‌دهد. برای سؤالات یا تحلیل اضافی، لطفاً به فایل‌های منبع مراجعه کنید یا با توسعه‌دهنده تماس بگیرید.*
